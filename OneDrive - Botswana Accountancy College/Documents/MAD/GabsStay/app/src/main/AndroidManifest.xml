<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.GabsStay"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.register.RegisterActivity" android:exported="false" />
        <activity android:name=".ui.listings.ListingsActivity" android:exported="false" />
        <activity android:name=".ui.filter.FilterActivity" android:exported="false" />
        <activity android:name=".ui.detail.ListingDetailActivity" android:exported="false" />
        <activity android:name=".ui.reservation.ReservationActivity" android:exported="false" />
        <activity android:name=".ui.chat.ChatActivity" android:exported="false" />

    </application>

</manifest>
